## What this changes

## Why

## Checklist
- [ ] `go build ./...` and `go test ./...` pass
- [ ] `gofmt -w .` run
- [ ] If it touches `proto/` (the security core), I've explained the reasoning
- [ ] Docs updated if behaviour changed

> Note: `main` and `release/*` require review + green CI before merge.
